import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // swcMinify: false, // Disable SWC minification
  // webpack: (config) => {
  //   // Disable minification plugins if needed
  //   config.optimization.minimize = false;
  //   return config;
  // },
};

export default nextConfig;
